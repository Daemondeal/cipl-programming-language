(if true (then (expression (call println good)) (else (expression (call println bad)))
(if false (then (expression (call println bad)) (else (expression (call println good)))
(if false (then (block
    pass
) (else (block
    (expression (call println block))
))

