(if true (then (if false (then (expression (call println bad)) (else (expression (call println good))))
(if false (then (if true (then (expression (call println bad)) (else (expression (call println bad))))

